import * as React from "react";
import styled from "styled-components";

import { ExportContainer } from "../../../styles/container";
const { FooterCompo } = ExportContainer;

const Footer = () =>{
    return(
        <FooterCompo>
            
        </FooterCompo>
    )
}

export default Footer