
import styled from "styled-components";
import { css } from "styled-components";

export const centerPlaced = css`
    top: 50%;
    left: 50%;
    transform:translate(-50% , -50%);
    position:absolute;
`;